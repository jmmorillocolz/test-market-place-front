export enum EStatusCode {
    Success = 200,
    InvalidRequest = 400,
    Forbidden = 401,
    NotFound = 404,
    UknownError = 500,
    BadGateway = 502,
    ServiceUnavailable = 503,
    GatewayTimeout = 504
}
